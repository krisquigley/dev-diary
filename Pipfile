[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-flatpages = "*"
frozen-flask = "*"
pygments = "*"
flask-parcel = "*"
importlib-metadata = "*"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
dev = 'python main.py'
build = 'bash -c "yarn install && yarn build && python main.py build"'
